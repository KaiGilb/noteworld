// UNIT_TYPE=Feature

/**
 * Vuex store for NoteWorld.
 *
 * Holds app-level state: session info, locale, search history, etc.
 * RDF graph data is NOT stored here — it lives in rdfStore.js (rdflib graph singletons).
 *
 * @see Standard: /Users/kaigilb/Vault_Ideas/9 - Standard/Rule_Code_vue-coding.md (STATE_MGMT rules)
 */

import { createStore } from 'vuex'

export default createStore({
  state: {},
  mutations: {},
  actions: {}
})
